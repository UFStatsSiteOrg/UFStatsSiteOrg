{
  "name": "my-project",
  "version": "0.1.0",
  "homepage": ".",
  "private": true,
  "scripts": {
    "serverinstall": "cd backend && npm install",
    "reactinstall": "cd react-app && npm install",
    "start": "node backend/index.js",
    "install": "npm run serverinstall && npm run reactinstall &&  npm install craco && cd react-app && craco build"
  }
}
