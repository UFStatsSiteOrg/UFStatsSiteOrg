{
  "name": "my-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serverinstall": "cd backend && npm install",
    "reactinstall": "cd react-app && npm install",
    "buildreact": "cd react-app && npm build",
    "start": "npm run buildreact && node backend/index.js",
    "install": "npm run serverinstall && npm run reactinstall"
  }
}
