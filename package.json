{
  "name": "my-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serverinstall": "cd backend && npm install",
    "reactinstall": "cd react-app && npm install",
    "build": "cd react-app && npm build",
    "start": "node backend/index.js",
    "install": "npm run serverinstall && npm run reactinstall && npm run build"
  }
}
