{
  "name": "my-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serverinstall": "yarn --cwd backend install",
    "reactinstall": "yarn --cwd react-app install",
    "build": "yarn --cwd react-app build"
  }
}
